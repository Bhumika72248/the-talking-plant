import React from "react";
import BeautifulHome from "../components/BeautifulHome";

const Home = ({ currentReading }) => {
  return <BeautifulHome currentReading={currentReading} />;
};

export default Home;
